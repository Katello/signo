# the location where signo is installed
SIGNO_HOME=/usr/share/signo

# the group which runs the web interface
SIGNO_GROUP=signo

# the user which runs the web interface
SIGNO_USER=signo

# signo rails environment
SIGNO_ENV=production
